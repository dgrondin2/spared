<!--
  - items/index.html.erb
  -
  - Displays the "item-offer board".
  -->

<%= render :partial => 'sessions/donordashnav' %>

<div class="whitespace clear"></div>

<%= render :partial => 'sessions/donormydash' %>




<!-- Some white space to separate content from footer a little bit more: -->
<div class="whitespace clear"></div>


<!-- Auto-generated rails code (for reference):
<h1>Listing items</h1>

<table>
  <tr>
    <th>Name</th>
    <th>City</th>
    <th>State</th>
    <th>Zip</th>
    <th>Category</th>
    <th>Animal type</th>
    <th>Condition</th>
    <th>Estimated value</th>
    <th>Expiration date</th>
    <th>Description</th>
    <th>Image</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= item.name %></td>
    <td><%= item.city %></td>
    <td><%= item.state %></td>
    <td><%= item.zip %></td>
    <td><%= item.category %></td>
    <td><%= item.animal_type %></td>
    <td><%= item.condition %></td>
    <td><%= item.estimated_value %></td>
    <td><%= item.expiration_date %></td>
    <td><%= item.description %></td>
    <td><%= item.image %></td>
    <td><%= link_to 'Show', item %></td>
    <td><%= link_to 'Edit', edit_item_path(item) %></td>
    <td><%= link_to 'Destroy', item, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Item', new_item_path %>

-->