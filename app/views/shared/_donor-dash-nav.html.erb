<div class='donor-dash-nav left-dash-nav'>
	<h2>DONATE:</h2>
	
	<!-- "Donate items" -->
	<% if current_page?(donor_item_offer_overview_path) %>
	    <%= link_to image_tag('donor-navigation/selected5cropped.png'), donor_item_offer_overview_path, class: 'active' %>
	<% else %>
		<%= link_to image_tag('donor-navigation/standard5cropped.png'), donor_item_offer_overview_path %>
	<% end %>
	
	<!-- "Donate money" -->
	<% if current_page?(donor_new_donation_path) %>
		<%= link_to image_tag('donor-navigation/selected4cropped.png'), donor_new_donation_path, class: 'active' %>
	<% else %>
		<%= link_to image_tag('donor-navigation/standard4cropped.png'), donor_new_donation_path %>
	<% end %>

	<!-- "Donate time" -->
	<% if current_page?(donor_new_event_path) %>
		<%= link_to image_tag('donor-navigation/selected3cropped.png'), donor_new_event_path, class: 'active' %>
	<% else %>
		<%= link_to image_tag('donor-navigation/standard3cropped.png'), donor_new_event_path %>
	<% end %>
</div>

<div class='donor-dash-nav right-dash-nav'>
	<h2>BROWSE:</h2>
	
	<!-- "Volunteer opportunities" -->
	<% if current_page?(browse_events_path) %>
		<%= link_to image_tag('donor-navigation/selectedcropped.png'), browse_events_path, class: 'active' %>
	<% else %>
		<%= link_to image_tag('donor-navigation/standardcropped.png'), browse_events_path %>
	<% end %>
	
	<!-- "Organization wishlists" -->
	<% if current_page?(browse_wishlists_path) %>
		<%= link_to image_tag('donor-navigation/selected1cropped.png'), browse_wishlists_path, class: 'active' %>
	<% else %>
		<%= link_to image_tag('donor-navigation/standard1cropped.png'), browse_wishlists_path %>
	<% end %>
	
	<!-- "Item-offer board" -->
	<% if current_page?(browse_item_offers_path) %>
		<%= link_to image_tag('donor-navigation/selected2cropped.png'), browse_item_offers_path, class: 'active' %>
	<% else %>
		<%= link_to image_tag('donor-navigation/standard2cropped.png'), browse_item_offers_path %>
	<% end %>
</div>