<%= render :partial => 'sessions/donordashnav' %>

<div class="whitespace clear"></div>

<%= render :partial => 'sessions/donormydash' %>


<!-- Some white space to separate content from footer a little bit more: -->
<div class="whitespace clear"></div>



<!-- Auto-generated rails code (for reference):
<h1>Listing wishlist_items</h1>

<table>
  <tr>
    <th>Quantity</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @wishlist_items.each do |wishlist_item| %>
  <tr>
    <td><%= wishlist_item.quantity %></td>
    <td><%= wishlist_item.description %></td>
    <td><%= link_to 'Show', wishlist_item %></td>
    <td><%= link_to 'Edit', edit_wishlist_item_path(wishlist_item) %></td>
    <td><%= link_to 'Destroy', wishlist_item, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Wishlist item', new_wishlist_item_path %>
-->